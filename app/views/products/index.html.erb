<p id="notice"><%= notice %></p>

<h1>Products</h1>

<table>
  <thead>
    <tr>
      <th>Imagen</th>
      <th>Sku</th>
      <th>Descripcion</th>
      <th>Precio</th>            
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <h2>Novedades</h2>
    <!-- Listar novedades-->
    <% @products.last(20).each do | prd | %>
    <tr>
      <td><%= image_tag prd.imagen.url(:thumb) %> | <%= prd.sku %> | <%= prd.descripcion %> | <%= prd.precio %></td> 
    </tr>
    <% end -%>
    <!-- fin novedades -->    
  </tbody>

  <!--<tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.sku %></td>
        <td><%= product.precio %></td>
        <td><%= product.descripcion %></td>
        <td><%= product.imagen %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>-->
</table>

<br>

<%= link_to 'New Product', new_product_path %>
